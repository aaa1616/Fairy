#ifndef _REPEAT_DNA_H
#define _REPEAT_DNA_H
#include "ftinclib.h"

char **findRepeatedDnaSequences(char *input, int *outputSize);
char **findRepeatedDnaSequencesTT(char *input, int *outputSize);
char *intToRoman(int num);

#endif // !_REPEAT_DNA_H
